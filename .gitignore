# Dependencias de Node.js — se reinstalan con: npm install
node_modules/

# Archivos de sistema de macOS
.DS_Store

# Archivos de secretos — NUNCA subir
.env
.env.local
.env.production

# Configuración MCP con tokens — NUNCA subir
.claude/mcp.json

# Base de datos SQLite — cada desarrollador crea la suya localmente
db/nexoai.db
db/nexoai.db-shm
db/nexoai.db-wal
